using System;

class Missing
{
    public static readonly DateTimeOffset DateTimeOffset = new(0917, 06, 01, 04, 02, 08, 003, TimeSpan.FromHours(2));
    public static readonly DateOnly Named = new(day: 02, month: 06, year: 0003);
}

