using System;

class Missing
{
    public static readonly DateTimeOffset DateTimeOffset = new(917, 6, 1, 4, 2, 8, 3, TimeSpan.FromHours(2));
    public static readonly DateOnly Named = new(day: 2, month: 6, year: 03);
}

